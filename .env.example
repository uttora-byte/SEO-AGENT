# SEO Agent — environment template
# Copy to .env.local. All values are OPTIONAL: API keys are normally entered
# via the Settings UI, which stores them in the local SQLite database.

# Override the SQLite file location (default: ./seo-agent.db at repo root)
# DATABASE_PATH=/absolute/path/to/seo-agent.db
